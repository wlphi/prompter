# Teleprompter Backend Configuration
# Copy this file to .env and adjust as needed

# Logging Configuration
# Valid values: DEBUG, INFO, WARNING, ERROR, CRITICAL
# - DEBUG: Detailed diagnostic information (development)
# - INFO: Confirmation that things are working (production default)
# - WARNING: Something unexpected but recoverable
# - ERROR: Serious problems requiring attention
LOG_LEVEL=INFO

# Server Configuration
# Host to bind to (0.0.0.0 = all interfaces, 127.0.0.1 = localhost only)
HOST=0.0.0.0

# Port to listen on
PORT=8000
